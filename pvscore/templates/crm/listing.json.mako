{
"listing_id" : "${listing.listing_id}",
"title" : "${listing.title}",
"location" : "${listing.location}",
"description" : "${listing.description}",
"create_dt" : "${listing.create_dt}",
"latitude" : "${listing.latitude}",
"longitude" : "${listing.longitude}"
}
