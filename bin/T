#!/bin/bash

rm -f .coverage

rm -rf ./data/cache ./data/sessions #./data/templates/crm/*

nosetests -x -v --exclude-dir-file=unittest-exclude.txt --cover-package=pvscore.model --cover-package=pvscore.controllers --cover-package=pvscore.lib --cover-package=pvscore.config $*
#nosetests -v $*

