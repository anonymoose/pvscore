
# Ensure python 2.7.x installed on local system.
python --version

# ensure dev libs for XML are installed
sudo yum -y install libxml2 libxml2-devel libxslt libxslt-devel

# ensure that the server can talk to github.  see git-notes.txt

#
# set up virtualenv of python 2.7, download everything and install libs.
#
cd /apps
mkdir pvs
chown -R kbedwell:web pvs

cd /apps/pvs
mkdir log
chown -R kbedwell:web log
wget 'https://raw.github.com/pypa/virtualenv/master/virtualenv.py'

python virtualenv.py --no-site-packages .
source bin/activate
easy_install pyramid

git clone git@github.com:anonymoose/pvscore.git
git clone git@github.com:anonymoose/pvs.git

cd pvscore
python setup.py develop
cd ../pvs
python setup.py develop

mkdir -p storage/sites
mkdir -p storage/companies

